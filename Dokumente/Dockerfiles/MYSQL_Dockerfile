# Verwenden des offiziellen MySQL-Docker-Images als Basis
FROM mysql:latest

# SQL-Skript in den Container kopieren
COPY init.sql /docker-entrypoint-initdb.d/

# Port freigeben, auf dem MySQL läuft (standardmäßig 3306)
EXPOSE 3306
